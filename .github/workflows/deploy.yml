name: Deploy to RDA1
 
on:
  push:
    branches:
      - main
 
jobs:
  deploy:
    runs-on: [self-hosted, windows-server]
 
    steps:
 
      - name: Actualizar repositorio local
        uses: actions/checkout@v4
        run: |
          cd E:\Proyectos\RD4\Ind_Op_Preventa_Escaneo
          git config --global --add safe.directory E:\Proyectos\RD4\Ind_Op_Preventa_Escaneo
          git fetch origin main
          git reset --hard origin/main